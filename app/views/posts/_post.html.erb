<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <h4> Posted by <%= post.user.email%></h4>
    <h2><%= post.title %></h2>
    <h6><%= pluralize(post.views, "view") %></h6>
  </p>
  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>
</div>
